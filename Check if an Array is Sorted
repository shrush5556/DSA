import java.util.*;
public class Solution {
    public static int isSorted(int n, int []a) {
        // Write your code here.
        int max = a[n-1];

        for(int i=n-2;i>=0;i--){

            if(a[i]>max){
              return 0;
            }
       }
       return 1;
    }
}
